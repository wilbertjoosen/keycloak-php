language: php
sudo: true
dist: trusty
php:
  - 5.6
  - 7.0
  - 7.1
services:
  - docker
before_script:
  - composer self-update
  - docker run d -p 127.0.0.1:8080:8080 -e KEYCLOAK_USER=admin KEYCLOAK_PASSWORD=admin jboss/keycloak